MERGE INTO locations_out a
    USING locations_tmp b
    ON a.XY = b.XY
WHEN MATCHED THEN
    UPDATE SET
        XY = b.XY,
        Neighborhood = b.Neighborhood
WHEN NOT MATCHED THEN
    INSERT(
        XY,
        Neighborhood
    )
    VALUES(
        b.XY,
        b.Neighborhood
    );
